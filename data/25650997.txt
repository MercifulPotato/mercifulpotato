Item(by='flaviocopes', descendants=None, kids=None, score=None, time=1609880561, title=None, item_type='comment', url=None, parent=25650298, text='Not sure about the Jupyter-specific issues but macOS comes with Python 2 installed, so you have to install Python separately and use the `python3` binary, or alias `python` to the `python3` binary.<p>Then remember to run pip using `python3 -m pip &lt;command&gt;` and use virtual environments (venv)<p>I stumbled on this a couple weeks ago and documented the process here <a href="https:&#x2F;&#x2F;flaviocopes.com&#x2F;python-installation-macos&#x2F;" rel="nofollow">https:&#x2F;&#x2F;flaviocopes.com&#x2F;python-installation-macos&#x2F;</a><p>I found the same problem with Ruby (old version preinstalled on macOS)')