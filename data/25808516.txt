Item(by='zemo', descendants=None, kids=[25810218, 25810405, 25809301], score=None, time=1610862684, title=None, item_type='comment', url=None, parent=25805158, text='usually for stores like this I just do this:<p><pre><code>    type Store interface {\n        GetTask(*Task) error\n    }\n</code></pre>\ninstead of having &quot;GetTaskByID&quot; and &quot;GetTaskByTag&quot; and whatnot. Then in the caller you just do this:<p><pre><code>    task := store.Task{ID: 5}\n    if err := db.GetTask(&amp;task); err != nil {\n        &#x2F;&#x2F; wahtever\n    }\n</code></pre>\n^ that gets the task by ID<p><pre><code>    task := store.Task{Tag: &quot;foo&quot;}\n    if err := db.GetTask(&amp;task); err != nil {\n        &#x2F;&#x2F; wahtever\n    }\n</code></pre>\n^ that gets the task by tag.')