Item(by='mehrdadn', descendants=None, kids=None, score=None, time=1608634493, title=None, item_type='comment', url=None, parent=25494219, text='The real rule turns out to be simple (although non-obvious): &quot;Declaration matches usage.&quot;<p>So, for example, if you want a pointer to a function returning a pointer to an array of 3 integers, you&#x27;d want<p><pre><code>  (*(*f)())[3 - 1]\n</code></pre>\nto evaluate to an int, so the declaration would be<p><pre><code>  int (*(*f)())[3];</code></pre>')