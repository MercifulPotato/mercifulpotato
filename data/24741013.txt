Item(by='t-vi', descendants=None, kids=None, score=None, time=1602356079, title=None, item_type='comment', url=None, parent=24738835, text='&gt; The solution to Python’s GIL bottleneck is not some trick, it is to stop using Python for data-path code.<p>At least for the PyTorch bits of it, using the PyTorch JIT works well. When you run PyTorch code through Python, the intermediate results will be created as Python objects (with GIL and all) while when you run it in TorchScript, the intermediates will only be in C++ PyTorch Tensors, all without the GIL.\nWe have a small comment about it in our PyTorch book in the section on what improvements to expect from the PyTorch JIT and it seems rather relevant in practice.')