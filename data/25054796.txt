Item(by='lmm', descendants=None, kids=None, score=None, time=1605060468, title=None, item_type='comment', url=None, parent=25046784, text='The mixing of the array access (which is another piece of language-level special-case syntax) with the properties is what makes it verbose - &quot;normally&quot; you could just do something like<p><pre><code>    config.lens(tabs[2].title) set &quot;Suspended&quot;\n</code></pre>\nAdmittedly that relies on a macro, but the macro is pretty lightweight syntax sugar - if you wanted to do it in 100% vanilla code you&#x27;d need a .lens at every step and a slightly more explicit way to name the &quot;properties&quot;, i.e.<p><pre><code>    config.lens(_.tabs).lens(_(2)).lens(_.title) set &quot;Suspended&quot;\n</code></pre>\n&gt; is it possible at least to pack it into a generic function and apply it to most&#x2F;all assignments?<p>I don&#x27;t quite understand? You can certainly write generic functions that work for any lens whose &quot;target&quot; is a given type.')