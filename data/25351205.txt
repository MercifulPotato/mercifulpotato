Item(by='runarberg', descendants=None, kids=None, score=None, time=1607460802, title=None, item_type='comment', url=None, parent=25342748, text='&gt; &gt; &gt; We can’t perform arithmetic on an Option&lt;int&gt; any more than we could on a List&lt;int&gt;.<p>Is actually kind of a weird point to make. There are number of ways to add together List&lt;int&gt; including:<p><pre><code>    listA.zip(listB).map(([a, b]) =&gt; a + b);\n</code></pre>\nSimilarly adding together two optionals could be done with:<p><pre><code>    optionalA.zip(optionalB).map(([a, b]) =&gt; a + b);\n</code></pre>\nOr if you would rather:<p><pre><code>    optionalA\n      .zip(optionalB)\n      .map(([a, b]) =&gt; a + b)\n      .or(optionalA)\n      .or(optionalB)</code></pre>')