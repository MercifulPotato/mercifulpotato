Item(by='hntemppony', descendants=None, kids=None, score=None, time=1611969220, title=None, item_type='comment', url=None, parent=25960063, text='Basically unaliasing distinguishes the cases:<p>* I have `iso^` because consumed the last reference to an `iso`, so this value can be sent anywhere or renamed<p>* I have `iso` because I&#x27;ve just evaluated a living reference to an `iso` object, so I can access fields and methods (with automatic receiver recovery) but can&#x27;t move it anywhere<p>Unaliasing with `consume` is what produces `iso^`')