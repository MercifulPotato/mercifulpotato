Item(by='dathinab', descendants=None, kids=[24672523], score=None, time=1601735845, title=None, item_type='comment', url=None, parent=24672238, text='Yes, but it&#x27;s more than that.<p>once_cell allows you more ways to do lazy initialization.<p>Like by using `once_cell::sync::OnceCell` instead of `once_cell::sync::Lazy` you can determine the init function when trying to access the once cell. For many cases `Lazy` is what you want but for some `OnceCell` makes it just so much easier.<p>And then you also can use the types outside of a &quot;lazy initialized static&#x2F;global&quot; use case.<p>So generally it&#x27;s better (by now).')