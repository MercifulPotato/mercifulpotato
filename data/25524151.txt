Item(by='anarazel', descendants=None, kids=None, score=None, time=1608771966, title=None, item_type='comment', url=None, parent=25524047, text='&gt; Did they change libaio to work without O_DIRECT at some point?<p>No, and I don&#x27;t really forsee that happening at this point.<p>&gt;  Or are you talking about io_uring for async file io?<p>Yep. io_uring can do async buffered file IO.<p>Initially, for buffered file IO, everything not in the page cache (e.g. a cache miss read, or a write without a page cache page already existing) was done via kernel threads inside the kernel, but that&#x27;s being incrementally improved. Now most buffered reads don&#x27;t need a kernel thread anymore (instead they are submitted during the io_uring_enter, and completed in task context, avoiding a lot of the overhead of &quot;synchronous&quot; execution in a kernel thread).')