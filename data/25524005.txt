Item(by='renewiltord', descendants=None, kids=[25524196], score=None, time=1608770388, title=None, item_type='comment', url=None, parent=25523874, text='Hey, thank you for offering. Yes! Something minimal like that that doesn&#x27;t use `reqwest::blocking` would be very helpful.<p>If it could do the Error case too that would be cool.<p>For Rocket, I have something like:<p><pre><code>    #[get(&quot;&#x2F;list&#x2F;&lt;status&gt;&quot;)]\n    fn list_prospects(api_key: ApiKey, status: String) -&gt; Result&lt;Json&lt;PendingList&gt;, Status&gt;\n</code></pre>\nwith a<p><pre><code>    impl&lt;&#x27;a, &#x27;r&gt; FromRequest&lt;&#x27;a, &#x27;r&gt; for ApiKey\n</code></pre>\nBut I couldn&#x27;t quickly get an `async` piece in there so I just sucked it up and synced it all up since it&#x27;s only backing a Retool dashboard so it isn&#x27;t the end of the world.<p>So if the example is like:<p><pre><code>    #[get(&#x27;&#x2F;todo&#x27;)]\n    fn get_todos() -&gt; Result&lt;Json&lt;TodoList&gt;, Status&gt;\n      let todos = reqwest...\n      Ok(todos)\n</code></pre>\nor the equivalent in the web framework you have that would be hecka useful.')