Item(by='yokaze', descendants=None, kids=None, score=None, time=1604644962, title=None, item_type='comment', url=None, parent=25005348, text='&gt; References can be copied\n&gt; But it doesn’t do the same thing as assigning to them<p>I think, that&#x27;s not a good mental model for references.\nA reference is an opaque data type.\nThe initial assignment is &quot;taking a reference&quot; and all later operations work the same, as you would refer to the referenced object itself.<p>So<p><pre><code>  int&amp; r2 = r1; &#x2F;&#x2F; r2 now points to i, like r1\n\n</code></pre>\nI would rather say<p><pre><code>   int&amp; r2 = r1; &#x2F;&#x2F; r2 now references i, like r1\n</code></pre>\nBecause it isn&#x27;t a pointer. It may be implemented as one (e.g. when passing arguments to functions), but in this case, I don&#x27;t think, even that will happen. You just have another name for the same object.')