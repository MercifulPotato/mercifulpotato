Item(by='karolist', descendants=None, kids=None, score=None, time=1601832732, title=None, item_type='comment', url=None, parent=24675373, text='Here&#x27;s how I did it, but I think there should be a more optimal solution.<p>Inspect history, see that mistake was made 1 commit ago<p><pre><code>  git log\n</code></pre>\nInteractive rebase to edit last 2 commits<p><pre><code>  git rebase -i HEAD~2\n</code></pre>\nThis will open rebase editor, change first line to below<p><pre><code>  edit e794bf2 Add Hello world\n  pick 77c37d2 Further work on Hello world\n</code></pre>\nFix typo in file.txt<p>stage modified file.txt and amend to commit<p><pre><code>  git add file.txt\n  git commit --amend\n\n</code></pre>\nIn the editor fix commit message (again?)<p>This causes merge conflict in file.txt, should be avoidable?<p><pre><code>  git rebase --continue\n</code></pre>\nEdit file.txt to remove conflict markers, make it as below<p><pre><code>  Hello world\n  Hello world is an excellent program.\n</code></pre>\nAgain, stage file.txt and continue, just exit the editor, no changes needed on the last commit.<p><pre><code>  git add file.txt\n  git rebase --continue\n</code></pre>\nYou can inspect with git log, but that&#x27;s it<p><pre><code>  git verify</code></pre>')