Item(by='geophile', descendants=None, kids=None, score=None, time=1607800133, title=None, item_type='comment', url=None, parent=25400582, text='Oh, and when you do bail out and go to Python, marcel&#x27;s operators are available in a module (marcel.api), so that you can take your pipeline and move it into Python easily. E.g. here&#x27;s the same example in Python:<p><pre><code>    from marcel.api import *\n\n    for path, line in (ls(file=True, recursive=True) |\n                       select(lambda f: f.suffix == &#x27;.py&#x27; and \n                              now() - f.mtime &lt; days(1)) |\n                       read(label=True) |\n                       select(lambda path, line: line.startswith(&#x27;class &#x27;))):\n        print(f&#x27;({path}: {line})\n                       read(label=True)</code></pre>')