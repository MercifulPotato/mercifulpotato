Item(by='DaiPlusPlus', descendants=None, kids=[25894306], score=None, time=1611505123, title=None, item_type='comment', url=None, parent=25891477, text='Is than an OS thread or a Loom-thread though?<p>If it’s an OS thread, then that won’t scale and defeats the point of using Loom (though it does still reduce the overall system load).<p>If it’s a Loom-thread then doing `.join()` is the same thing as doing an `await` - in which case it’s silly to not just have a `.dontJoinJustYet()` method that’s available for every Loom promise.')