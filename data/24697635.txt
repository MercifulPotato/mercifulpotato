Item(by='im3w1l', descendants=None, kids=None, score=None, time=1601992245, title=None, item_type='comment', url=None, parent=24697084, text='&gt;  In consensus systems, however, any difference in behaviour can be a fatal vulnerability.<p>Another issue can come if you have code like this, and different validations are used.<p><pre><code>    if (signature_valid()) {\n      &#x2F;&#x2F; Safe because we already verified signature.\n      use_signed_message_or_die();\n    }\n</code></pre>\nIn practice the check and usage could be very far apart in the code (maybe using the type system to keep track of verified signatures), or even in different programs.')