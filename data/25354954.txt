Item(by='judge2020', descendants=None, kids=None, score=None, time=1607484005, title=None, item_type='comment', url=None, parent=25353122, text='So if your DNS request is encrypted to the resolver, and from the resolver to a second resolver (first resolver is ODOH proxy), then is unencrypted from that resolver to the authoritative nameserver, where does the ISP get to see your DNS query? Unless you mean SNI, which is its own thing being worked on[0] (yes, it only works for big CDNs to look benign and you probably could still correlate IPs via traffic patterns. Doesn&#x27;t mean it&#x27;ll be as easy as it is now, though).<p>0: <a href="https:&#x2F;&#x2F;blog.cloudflare.com&#x2F;encrypted-client-hello&#x2F;" rel="nofollow">https:&#x2F;&#x2F;blog.cloudflare.com&#x2F;encrypted-client-hello&#x2F;</a>')