Item(by='mmis1000', descendants=None, kids=None, score=None, time=1607478527, title=None, item_type='comment', url=None, parent=25341624, text='That is where you use optional chaining + extension method. And that is basically identical to Optional.map or whatever method in those language with Optional type<p>in kotlin<p><pre><code>  func add3 (val: Int?) = val ?.let { it + 3 }\n</code></pre>\nThe language gives you a way to say: `this value plus 3 if it exist or just return null`<p>Unfortunately, there is no such method in js currently, so it looks half baked.')