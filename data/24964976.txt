Item(by='a1369209993', descendants=None, kids=[24973244], score=None, time=1604293588, title=None, item_type='comment', url=None, parent=24963404, text='&gt; base + UINT_MAX+1 must access base + 0 when the index is `unsigned int`, even on a 64-bit machine.<p>Ironically, given the topic, that&#x27;s actually <i>not</i> true, because `base + UINT_MAX+1` is `(base + UINT_MAX)+1` (with a pointer, not a unsigned int, as the temporary value). That should probably be `base + (UINT_MAX+1)`.')