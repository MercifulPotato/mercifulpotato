Item(by='ptx', descendants=None, kids=[25265026], score=None, time=1606783730, title=None, item_type='comment', url=None, parent=25260776, text='It should be the same process, except that the constraints file is not automatically created or detected, so step 5 would be &quot;pip freeze &gt;constraints.txt&quot; and step 6 would be &quot;pip install -r requirements.txt -c constraints.txt&quot;.<p>The top level dependencies go in requirements.txt and trigger installation of those packages. Everything else goes in the constraints file, which constrains the version that will be installed if something triggers an installation of the package, but it doesn&#x27;t by itself trigger the installation - it only locks&#x2F;constrains the versions.')