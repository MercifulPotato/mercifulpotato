Item(by='henrikeh', descendants=None, kids=[25255102], score=None, time=1606750854, title=None, item_type='comment', url=None, parent=25253650, text='Yes, the symbolic functions can be made &quot;retroactively&quot;. The various conventional functions are overloaded with the symbolic input.<p><pre><code>    &gt;&gt; syms x\n    &gt;&gt; f = @(x) log(sqrt(x)).^2\n\n    f = function_handle with value:\n\n        @(x)log(sqrt(x)).^2\n\n    &gt;&gt; f(x)\n \n    ans = log(x^(1&#x2F;2))^2\n \n    &gt;&gt; finverse(f(x))\n \n    ans = exp(2*x^(1&#x2F;2))\n</code></pre>\nAnd to implement under:<p><pre><code>    function u = under(f, g)\n\n    syms x\n\n    g_inv = matlabFunction(finverse(g(x)));\n\n    u = @(x) g_inv(f(g(x)));\n\n    end</code></pre>')