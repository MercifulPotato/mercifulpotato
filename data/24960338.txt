Item(by='mhils', descendants=None, kids=None, score=None, time=1604246653, title=None, item_type='comment', url=None, parent=24960262, text='You can view TCP contents in the mitmproxy UI since a few months (thanks to some excellent community contributions). Minimal example:<p><pre><code>  mitmproxy --mode reverse:http:&#x2F;&#x2F;example.com --tcp-hosts example.com\n  curl http:&#x2F;&#x2F;localhost:8080&#x2F;\n</code></pre>\nThere&#x27;s still quite a bit we want to improve on (e.g., our protocol detection currently fails for protocols that start with a server-side greeting), but the basic TCP viewing functionality is there. :)')