Item(by='a1369209993', descendants=None, kids=[25562907, 25562736], score=None, time=1609183424, title=None, item_type='comment', url=None, parent=25561146, text='&gt; There are other strange things in the IO monad, such as the ability to observe evaluation orders by throwing exceptions.<p>That&#x27;s not specific to IO; you can more or less always observe evaluation order by throwing exceptions:<p><pre><code>  bar = error &quot;bar&quot;\n  baz = error &quot;baz&quot;\n  foo = (bar,baz) :: (Int,Int)\n  main = print foo -- well, I guess you need IO to run anything at all\n</code></pre>\n`bar` is evaluated first (at least on my machine), as evidenced by:<p><pre><code>  $ .&#x2F;test\n  test: bar\n  CallStack (from HasCallStack):\n    error, called at test.hs:1:7 in main:Main</code></pre>')