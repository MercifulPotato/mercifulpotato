Item(by='heyitsguay', descendants=None, kids=None, score=None, time=1603044207, title=None, item_type='comment', url=None, parent=24814422, text='If you add a `uniform vec3 mousePrevFrame` that tracks the previous mouse position, you can get smoother drawing in main() by replacing your distance function in the line `float dist = distance(mouse.xy, gl_FragCoord.xy)` with a custom<p><pre><code>  distanceToLine(mousePrevFrame.xy, mouse.xy, gl_FragCoord.xy) </code></pre>\ndefined as<p><pre><code>  float distanceToLine(vec2 p1, vec2 p2, vec2 p0) {\n    float d12 = distance(p1, p2);\n    if (d12 == 0) {\n      return distance(p1, p0);\n    }\n    return abs((p2.y - p1.y) * p0.x - (p2.x - p1.x) * p0.y + p2.x * p1.y - p2.y * p1.x) &#x2F; d12;\n    }</code></pre>')