Item(by='rualca', descendants=None, kids=None, score=None, time=1609916869, title=None, item_type='comment', url=None, parent=25652250, text='I agree, the author is mixing up presentation with the data type itself, and then pinning the blame on the poor presentation on the way he chose to present the data.<p>If the author&#x27;s problem is not being able to grep logs easily with UUIDs because they miss context info then the solution to his problem is to output logs that are searchable by providing additional context.<p>His arguments regarding searchability are also specious. Take for example<p>&gt; For example, a task ID would look like: `job-123-task-1`. This also helps in ad-hoc database queries to find relevant rows without complex JOINs.<p>Well, if he&#x27;s already running SQL queries then running an exact match on two attributes is far easier and elegant and less error prone than running a string pattern search on a derived  attribute.')