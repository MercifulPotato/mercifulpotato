Item(by='stinos', descendants=None, kids=[25012162], score=None, time=1604682168, title=None, item_type='comment', url=None, parent=25008824, text='<i>Use &quot;-e&quot; to set ssh options, just like with scp</i><p>On my rsync -e sets the executable, and my scp doesn&#x27;t have -e but -o. Even then it&#x27;s a bit of a stretch (or I&#x27;m missing something); e.g. for the ssh option I use mst of the time, a non-default port:<p><pre><code>     scp -P 4222\n</code></pre>\nis equivalent to<p><pre><code>     scp -o&quot;Port=4222&quot;\n</code></pre>\nis equivalent to<p><pre><code>     rsync -e &quot;ssh -p 4222&quot;\n</code></pre>\nAgain, this is just what I use after having to look it up at one point so I might be missing something, but it&#x27;s not like the arguments translate one to one?')