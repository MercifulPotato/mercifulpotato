Item(by='chrismorgan', descendants=None, kids=[25799452], score=None, time=1610749300, title=None, item_type='comment', url=None, parent=25797182, text='And another comparison, SimpleTimer.svelte:<p><pre><code>  &lt;script&gt;\n    let seconds = 0;\n\n    setInterval(() =&gt; {\n      seconds++;\n    }, 1000);\n  &lt;&#x2F;script&gt;\n\n  &lt;div&gt;{seconds} secs have elapsed...&lt;&#x2F;div&gt;\n</code></pre>\nNote that this deliberately functions slightly differently: it uses setInterval rather than repeated setTimeouts. This makes a much more accurate seconds counter, as setTimeout will wait for <i>at least</i> 1000 milliseconds before incrementing seconds and setting another timeout, meaning that you will <i>always</i> count time too slowly, commonly around 1% too slow, but more at times like if JavaScript execution is being throttled because the page is in the background. If you really wanted to do it the other way, it’d be something like this:<p><pre><code>  &lt;script&gt;\n    let seconds = 0;\n\n    function increment() {\n      seconds++;\n      setTimeout(increment, 1000);\n    }\n    setTimeout(increment, 1000);\n  &lt;&#x2F;script&gt;\n\n  &lt;div&gt;{seconds} secs have elapsed...&lt;&#x2F;div&gt;</code></pre>')