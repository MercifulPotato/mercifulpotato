Item(by='auganov', descendants=None, kids=[25343998], score=None, time=1607425970, title=None, item_type='comment', url=None, parent=25343084, text='These two examples are pretty different unless I&#x27;m not getting something?<p>The equivalent of the first in Clojure would be (assuming it&#x27;s js):<p><pre><code>  (doseq [i (range 3)]\n     (x i)\n     (y i)\n     (z i))\n</code></pre>\nAnd the equivalent of the Clojure in the second example in js (assuming we had range()) would be:<p><pre><code>   range(3).map(i=&gt;z(y(x(i))))\n\n</code></pre>\nIf you&#x27;re using a map as a for loop in js you wouldn&#x27;t have much indication of that either. And no idea how to properly indent these applications.<p>Like this perhaps?<p><pre><code>   range(3).map(\n     i=&gt;\n     z(\n       y(\n         x(i))))\n</code></pre>\nIMO single line is better with x,y,z. But perhaps with longer function names that&#x27;s what you would do.')