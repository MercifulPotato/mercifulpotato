Item(by='andy_threos_io', descendants=None, kids=[25328389], score=None, time=1607295510, title=None, item_type='comment', url=None, parent=25326552, text='Pointless and bad article.<p>I compiled the presented code out curiosity on arm gcc 8.2  on godbolt.org with -O3 op and the results are:<p>(the original remove is even faster then the so called elegant or the elegant with inline)<p><pre><code>  remove_cs101:\n          ldr     r2, [r0]\n          cmp     r2, r1\n          bne     .L3\n          b       .L9\n  .L6:\n          mov     r2, r3\n  .L3:\n          ldr     r3, [r2, #4]\n          cmp     r1, r3\n          bne     .L6\n          ldr     r3, [r1, #4]\n          str     r3, [r2, #4]\n          bx      lr\n  .L9:\n          ldr     r3, [r2, #4]\n          str     r3, [r0]\n          bx      lr\n  \n  \n  remove_elegant:\n          ldr     r2, [r0]\n          cmp     r1, r2\n          bne     .L12\n          b       .L13\n  .L14:\n          mov     r2, r3\n  .L12:\n          ldr     r3, [r2, #4]\n          cmp     r1, r3\n          bne     .L14\n          add     r0, r2, #4\n  .L13:\n          ldr     r3, [r1, #4]\n          str     r3, [r0]\n          bx      lr\n  \n  remove_elegant_with_inline:\n          ldr     r2, [r0]\n          cmp     r1, r2\n          cmpne   r2, #0\n          bne     .L17\n          b       .L18\n  .L19:\n          mov     r2, r3\n  .L17:\n          ldr     r3, [r2, #4]\n          cmp     r1, r3\n          cmpne   r3, #0\n          bne     .L19\n          add     r0, r2, #4\n  .L18:\n          ldr     r3, [r1, #4]\n          str     r3, [r0]\n          bx      lr\n\n</code></pre>\nEDIT: Hmmm, so much downvote without any reply.<p>Well if your code needs an article to be explained, rather than the plain solution and brings nothing, even 1 instruction slower, as the compiler can&#x27;t make the same (faster) result as the plain solution, than your code is pointless. And stating that this is the elegant solution is IMO bad practice.')