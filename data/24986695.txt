Item(by='pjmlp', descendants=None, kids=None, score=None, time=1604469742, title=None, item_type='comment', url=None, parent=24985026, text='The same API on Windows does a Windows call under the hood, on IBM I does a TIMI call under the hood and on PTC bare metal it does the work itself.<p>Android ported to any of those platforms would still make use of File(&quot;myfile&quot;).createNewFile() with zero dependencies on a Linux implementation.<p>No it doesn&#x27;t, the API call is still valid in Android 11, as long as the filesystem reserved for the application is used.<p>Even so, it has to do with the Java implementation.')