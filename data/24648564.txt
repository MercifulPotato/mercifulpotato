Item(by='makapuf', descendants=None, kids=[24649134, 24648911, 24650626], score=None, time=1601539468, title=None, item_type='comment', url=None, parent=24648511, text='IIUC you have this using `ranges` (which itself needs concepts as implemented). With it you can even do :<p><pre><code>    for (int i : ints | std::views::filter(even) | std::views::transform(square)) {\n        std::cout &lt;&lt; i &lt;&lt; &#x27; &#x27;;\n    }\n</code></pre>\nnotice the `for (int i:ints)` and the pipes filters.<p>EDIT: more specifically, you can use sort on a range now (source : <a href="https:&#x2F;&#x2F;cppreference.com" rel="nofollow">https:&#x2F;&#x2F;cppreference.com</a>):<p><pre><code>    ranges::sort(s);</code></pre>')