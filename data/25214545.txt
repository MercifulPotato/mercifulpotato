Item(by='Gibbon1', descendants=None, kids=None, score=None, time=1606339982, title=None, item_type='comment', url=None, parent=25204899, text='One feature I&#x27;ve wanted is an error return keyword to complement &#x27;return val;&#x27; something like &#x27;error_return val;&#x27; Then a feature where if a function does an error return but the caller doesn&#x27;t check the error the compiler inserts a exception or a panic.<p>So you could write code like<p><pre><code>  log_stuff(stuff);  &#x2F;&#x2F; on an error will panic.\n\n  var rtn = log_stuff(stuff);\n  if(rtn.error)\n  {\n    &#x2F;&#x2F; handle error\n  }\n\n  try\n  {\n     log_stuff(stuff);\n  }\n  catch()\n  {\n     &#x2F;&#x2F; jumps here on an error\n  }</code></pre>')