Item(by='4WIW', descendants=None, kids=[24721316], score=None, time=1602170292, title=None, item_type='comment', url=None, parent=24717838, text='To make this code clearer:<p><pre><code>    def get_users(f) -&gt; Tuple[List, List]:\n       return (logged_in_users(f), logged_out_users(f))\n\n    def user_count(f) -&gt; int:\n       return len(get_users())\n</code></pre>\nNow it&#x27;s clear this is just wrong code. To fix it:<p><pre><code>    def get_users(f) -&gt; List:\n       return logged_in_users(f) + logged_out_users(f)\n\n    def user_count(f) -&gt; int:\n       return len(get_users())</code></pre>')