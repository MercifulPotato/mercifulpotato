Item(by='chriswarbo', descendants=None, kids=None, score=None, time=1608399249, title=None, item_type='comment', url=None, parent=25477149, text='&gt; Simple: use mocker.patch to patch out random.randrange<p>Why not just pass in &#x27;randrange&#x27; as a function argument?<p><pre><code>    def findBias(randrange):\n      heads = tails = 0\n      for i in range(n):\n        if randrange(100) &lt; 100*p:\n          heads += 1\n        else:\n          tails += 1\n      return heads&#x2F;tails\n\n    import random\n    bias = findBias(random.randrange)\n\n    def myTest():\n      result = findBias(lambda: generateInSomeWay)\n      assert checkInSomeWay(result)\n</code></pre>\nIt&#x27;s amazing to me how many complicated and convoluted hacks people can come up with, to avoid calling a function with an argument.')