Item(by='nickjj', descendants=None, kids=None, score=None, time=1611954505, title=None, item_type='comment', url=None, parent=25962038, text='Did you run sudo apt-get update first?<p>I&#x27;m running WSL 2 here with Ubuntu 20.04 LTS and this is what worked for me, without having to install anything from source.<p>Without doing anything (vulnerable):<p><pre><code>    nick:~ $ sudoedit -s &#x27;\\&#x27; `perl -e &#x27;print &quot;A&quot; x 65536&#x27;`\n    malloc(): corrupted top size\n    Aborted\n</code></pre>\nThen I ran this to patch it:<p><pre><code>    sudo apt-get update &amp;&amp; sudo apt-get --only-upgrade install sudo\n</code></pre>\nAnd now this is the output (I guess not vulnerable anymore):<p><pre><code>    nick:~ $ sudoedit -s &#x27;\\&#x27; `perl -e &#x27;print &quot;A&quot; x 65536&#x27;`\n    usage: sudoedit [-AknS] [-r role] [-t type] [-C num] [-g group] [-h host] [-p prompt] [-T timeout] [-u user] file ...\n\n</code></pre>\nAfter patching it `dpkg -l | grep sudo` reports 1.8.31-1ubuntu1.2. I didn&#x27;t think to run that before patching it but I&#x27;d guess it&#x27;s going to be different.')