Item(by='asicsp', descendants=None, kids=[25026289, 25025477], score=None, time=1604839081, title=None, item_type='comment', url=None, parent=25025297, text='If you remove `cat`, it gives me nearly identical run time on my machine. I used `shuf -r -n10000000 -i 1-3 &gt; t1` as input. Also, output won&#x27;t be same, as the perl solution retains input order. `sort -nu` is faster than `sort+uniq` combo.<p><pre><code>    perl -ne &#x27;print if !$h{$_}++&#x27;\n</code></pre>\nruns faster (but this might depend on input, not sure if it is always faster than uniq) than the `sort -u` or `sort+uniq` combo<p>what&#x27;s surprising to me is that this was faster than<p><pre><code>    awk &#x27;!a[$0]++&#x27;</code></pre>')