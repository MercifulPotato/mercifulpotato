Item(by='superkuh', descendants=None, kids=None, score=None, time=1611548050, title=None, item_type='comment', url=None, parent=25895466, text='<p><pre><code>    sudo apt-get install nginx\n</code></pre>\nCheck where nginx puts the www directory by doing a google search, &quot;myos version nginx default directory&quot;.<p><pre><code>    cp index.html &#x2F;var&#x2F;www&#x2F;\n    cp latestphoto.jpg &#x2F;var&#x2F;www&#x2F;\n    # or use your OS GUI file manager\n</code></pre>\nSet up the port forward from the router to localip:80 and you&#x27;re done.<p>You&#x27;re assuming you need HTTPS but it&#x27;s a personal static site so no letsencrypt is needed yet. You&#x27;re assuming they need to configure nginx, but they don&#x27;t the debian&#x2F;ubuntu defaults are fine. You&#x27;re assuming they need to learn version management for some unfathomable reason but I can&#x27;t even understand that. You&#x27;re assuming people need some sort of &#x27;sync&#x27; but they don&#x27;t. That&#x27;s what an OS file manager is for.<p>It&#x27;s really not that complex unless you make it so.')